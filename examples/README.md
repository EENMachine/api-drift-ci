# Examples

## `spec/openapi.yaml`

Small **OpenAPI 3** document used by this repository’s dogfood workflow (`.github/workflows/openapi-drift.yml`). Change it in a pull request to see **API Drift CI** post a sticky comment on your PR.

## `../fixtures/`

Local-only fixtures for maintainers experimenting with [oasdiff](https://github.com/oasdiff/oasdiff); not used by the published Action by default.
