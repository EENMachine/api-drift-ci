openapi: 3.0.3
info:
  title: Example API (api-drift-ci fixture)
  version: "1.0.0"
paths:
  /pets:
    get:
      summary: List pets
      operationId: listPets
      responses:
        "200":
          description: OK
