# GitHub repository settings (copy-paste)

Apply these on **github.com → your repo → ⚙ About →** (gear) so the project looks credible before Marketplace review.

## Description (short blurb)

```text
OpenAPI diff on every pull request: sticky PR comment + optional CI fail on breaking changes (oasdiff). GitHub Action + reusable workflow.
```

## Website (optional)

Leave blank, or set to your org homepage if you prefer.

## Topics (suggested)

Add each as a topic (not one long string):

`openapi` `openapi3` `swagger` `github-actions` `api` `ci` `continuous-integration` `pull-request` `developer-experience` `static-analysis`

## Social preview

Upload a **1280×640** image under **Settings → General → Social preview** showing:

- Product name **API Drift CI**
- Subtext: *OpenAPI drift checks on pull requests*
- Optional: cropped screenshot of a real PR comment from this action

## Release badge (README)

Already included in README via shields.io. After Marketplace listing, you may add:

```markdown
[![GitHub Marketplace](https://img.shields.io/badge/Marketplace-API%20Drift%20CI-blue?logo=github)](https://github.com/marketplace/actions/YOUR-LISTING-SLUG)
```

Replace `YOUR-LISTING-SLUG` with the slug GitHub assigns after approval (edit README in a follow-up commit).
